{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "main-dev": "nodemon",
    "organization-dev": "yarn workspace @whitepaper/organization dev",
    "build": "yarn --cwd packages/main build",
    "start": "yarn --cwd packages/main start",
    "heroku-postbuild": "yarn --cwd packages/main build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^0.21.0",
    "next": "^9.2.0",
    "vhost": "^3.0.2"
  },
  "resolutions": {
    "react-dev-utils": "10.0.0"
  },
  "devDependencies": {
    "@types/node": "^13.1.8",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  }
}
